{
  "name": "@traxac/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "dev": "tsc --noEmit --watch --preserveWatchOutput",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "generate": "tsc -p tsconfig.json && drizzle-kit generate",
    "migrate": "tsx src/migrate.ts",
    "push": "tsc -p tsconfig.json && drizzle-kit push",
    "studio": "tsc -p tsconfig.json && drizzle-kit studio"
  },
  "dependencies": {
    "drizzle-orm": "^0.38.4",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.6",
    "typescript": "^5.5.4",
    "tsx": "^4.19.2",
    "@types/node": "^20"
  }
}