{
  "name": "@traxac/gst-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "tsc --noEmit --watch --preserveWatchOutput",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@traxac/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.5.4"
  }
}
